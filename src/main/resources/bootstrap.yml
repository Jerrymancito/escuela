spring:
  application:
    name: jdbc-template
  cloud:
    config:
      uri: http://localhost:9000
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    url: jdbc:h2:mem:testdb
